name: Disabled — Fly deploy (removed)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Fly deploy workflow disabled — Fly is no longer used."
